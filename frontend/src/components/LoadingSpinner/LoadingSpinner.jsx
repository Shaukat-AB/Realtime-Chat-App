const LoadingSpinner = () => {
  return (
    <div className="absolute w-full flex justify-center">
      <span className="h-screen loading loading-spinner loading-lg"></span>
    </div>
  );
};

export default LoadingSpinner;
